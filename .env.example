# DormMate 프로젝트 환경변수 예시
# 이 파일을 .env.local로 복사하고 실제 값으로 수정하세요

# ===========================================
# 데이터베이스 설정
# ===========================================
DATABASE_URL=jdbc:postgresql://localhost:5432/dormitory_db
DB_USERNAME=dorm_user
DB_PASSWORD=dorm_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dormitory_db

# ===========================================
# Redis 설정
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# JWT 설정
# ===========================================
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000
JWT_TTL_MIN=20

# ===========================================
# CORS 설정
# ===========================================
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# Rate Limiting
# ===========================================
RATE_LIMIT_REQ_PER_MIN=60

# ===========================================
# 로깅 설정
# ===========================================
LOG_LEVEL=json
TZ=UTC

# ===========================================
# 서버 설정
# ===========================================
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=local

# ===========================================
# AWS S3 설정 (선택사항)
# ===========================================
AWS_S3_BUCKET=dormmate-dev-storage
AWS_S3_REGION=ap-northeast-2
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# ===========================================
# 관리자 계정 설정 (선택사항)
# ===========================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
SPRING_SECURITY_USER=admin
SPRING_SECURITY_PASS=admin

# ===========================================
# 데모 계정 설정 (개발용)
# ===========================================
DEMO_USER1_HASH=demo-hash-1
DEMO_USER2_HASH=demo-hash-2

# ===========================================
# pgAdmin 설정 (선택사항)
# ===========================================
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
