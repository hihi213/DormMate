# DormMate .env.example
# 이 파일을 .env 로 복사한 뒤 실제 값으로 채워 사용하세요.
# 민감 값은 절대 커밋하지 마세요(secrets/ 또는 배포 비밀에 보관).

# ===========================================
# Database (docker-compose 기준)
# postgres://<user>:<password>@<host>:<port>/<db>
# 서비스명 'db'를 호스트로 사용하려면 컨테이너 내부에서 접근할 때만 사용합니다.
# 로컬 호스트에서 접근 시 host=localhost 권장
# ===========================================
DATABASE_URL=postgres://dorm_user:dorm_password@localhost:5432/dormitory_db

# ===========================================
# JWT
# ===========================================
JWT_SECRET=replace_with_random_string
JWT_TTL_MIN=20

# ===========================================
# CORS & Rate Limiting
# ===========================================
CORS_ALLOWED_ORIGINS=http://localhost:5173
RATE_LIMIT_REQ_PER_MIN=60

# ===========================================
# Logging & Timezone
# ===========================================
LOG_LEVEL=json
TZ=UTC

# ===========================================
# Seeding (optional)
#  - 관리자 계정 자동 생성 시 bcrypt/argon2 해시를 주입
# ===========================================
ADMIN_PASSWORD_HASH=

# ===========================================
# Optional: pgAdmin overrides (docker-compose 기본값 존재)
# ===========================================
#PGADMIN_EMAIL=admin@example.com
#PGADMIN_PASSWORD=admin
